NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_DEFAULT_LOCALE=sr

# Cron endpoint auth (Vercel Cron sends Authorization: Bearer <CRON_SECRET>)
CRON_SECRET=

# Optional — booking notification emails (Phase 5)
RESEND_API_KEY=
RESEND_FROM_EMAIL=PlaySlot <onboarding@resend.dev>

# Supabase Dashboard → Authentication → URL Configuration (localhost dev):
# Site URL: http://localhost:3000
# Redirect URLs: http://localhost:3000/** , http://localhost:3000/auth/callback
